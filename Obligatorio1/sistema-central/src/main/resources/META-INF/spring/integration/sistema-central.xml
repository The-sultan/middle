<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:int-stream="http://www.springframework.org/schema/integration/stream"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/integration
		http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/stream
		http://www.springframework.org/schema/integration/stream/spring-integration-stream.xsd">

	<int:annotation-config/>

	<!-- Bean del conversor de Farenheit a Celsius, TESTING ONLY -->
	<int:gateway id="tempTest" service-interface="uy.edu.fing.inco.lins.TempConverter"/>

	<context:component-scan base-package="uy.edu.fing.inco.lins"/>
	<!-- Pago del cliente (o de la sucursal, es lo mismo) que dispara el flujo de Spring Integration -->
	<int:gateway id="pago" service-interface="uy.edu.fing.inco.lins.PagoCliente"/>

	<!-- Canal que recibe un pago y convierte en una orden de pago -->
	<int:channel id="ordenes"/>
	<!-- Canal que divide los pagos en Items de pago individuales para el Router de pagos -->
	<int:channel id="pagoIndividual"/>
	<!-- Canales que reciben el input del Router de pagos -->
	<int:channel id="pagoEntradas"><int:queue capacity="10"/></int:channel>
	<int:channel id="pagoFacturas"><int:queue capacity="10"/></int:channel>
	<int:channel id="pagoOffline"><int:queue capacity="10"/></int:channel>

	<!-- Canales que envian info al ProcesaOrdenPago  -->
	<int:channel id="partnerFacturas"/>
	<int:channel id="partnerEntradas"/>
	<int:channel id="partnerOffline"/>
	<!-- Canal que consolida los TicketPago cuando se procesan -->
	<int:channel id="ticketChannel"/>
	<!-- Pasamano para que el ProcesaOrdenPago sea mas legible -->
	<int:bridge input-channel="pagoEntradas" output-channel="partnerEntradas">
		<int:poller fixed-delay="1000"/>
	</int:bridge>
	<int:bridge input-channel="pagoFacturas" output-channel="partnerFacturas">
		<int:poller fixed-delay="1000"/>
	</int:bridge>
	<int:bridge input-channel="pagoOffline" output-channel="partnerOffline">
		<int:poller fixed-delay="1000"/>
	</int:bridge>
	<!-- Canal unico de respuesta a la sucursal que emitio el pago  -->
	<int-stream:stdout-channel-adapter id="responseChannel"/>

</beans:beans>
